rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read & write access for a seamless prototype/preview experience
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
